language: perl
perl:
        - "5.16"
before_install:
        - sudo apt-get install -y libswitch-perl
        - sudo apt-get install -y cyrus-imapd cyrus-clients cyrus-admin sasl2-bin
addons:
          apt:
                update: true
                source:
                        - sourceline: 'deb http://it.archive.ubuntu.com/ubuntu/ eoan main universe'
install:
        true

after_failure:
          - cat /home/travis/.cpanm/work/*/build.log
